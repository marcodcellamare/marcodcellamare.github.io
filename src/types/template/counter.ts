export interface Counter {
	PREFX?: string;
	SUFFX?: string;
}
export interface CounterTemplate {
	since: string;
	className?: string;
	classNamePre?: string;
	classNamePost?: string;
	newLineAt?: string;
}
